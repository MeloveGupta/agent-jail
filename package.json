{
  "name": "agent-jail",
  "version": "1.0.0",
  "description": "Policy enforcement layer for AI agent tool execution using AWS Cedar",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/verify-cedar.js && node test/verify-runtime.js && node test/verify-server.js && node test/verify-frontend.js && node test/verify-integration.js && node test/verify-security.js && node test/verify-api-boundary.js && node test/verify-browser-adversarial.js"
  },
  "dependencies": {
    "@cedar-policy/cedar-wasm": "^4.0.0",
    "express": "^4.19.2"
  }
}
